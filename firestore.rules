rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORAL: Permitir todo en desarrollo
    // CAMBIAR EN PRODUCCIÓN por reglas más restrictivas
    match /{document=**} {
      allow read, write: if true;
    }
    
    // ESTRUCTURA PARA PRODUCCIÓN (comentada):
    // match /usuarios/{userType}/{userId} {
    //   allow read: if request.auth != null;
    //   allow write: if request.auth != null && request.auth.uid == userId;
    // }
  }
}